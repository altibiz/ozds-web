@page "{secondarySiteContentItemId}"

@using Ozds.Util

@{
  ViewLayout = "LayoutDashboard";
}

@(
await Orchard.DisplayAsync(
  await Orchard.GetContentItemByIdAsync(
    Request.RouteValues["secondarySiteContentItemId"] as string),
  "Dashboard")
)
