@{
  TagBuilder tag = Tag(Model, "ul");
  tag.AddCssClass("row justify-content-center list-unstyled");

  foreach (var item in Model.Items)
  {
    tag.InnerHtml.AppendHtml(await DisplayAsync(item));
  }
}

@tag