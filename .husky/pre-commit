#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

HUSKY_DIR="$(dirname "$(realpath "${BASH_SOURCE[0]}")")";
ROOT_DIR="$(dirname "$HUSKY_DIR")";

"$ROOT_DIR/scripts/build";
"$ROOT_DIR/scripts/format";
git add "$ROOT_DIR";
